

document.addEventListener('click', dropListener)

// if (document.querySelector(e.target.dataset.drop).classList.contains('active')) e.target.close()
// else e.target.open()
// setTimeout(() => {
//     document.addEventListener('click', e => dropListener(e))
// }, 200);